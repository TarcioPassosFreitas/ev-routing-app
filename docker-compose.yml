version: "3.9"

services:
  frontend:
    build: .
    ports:
      - "5173:5173"
    networks:
      - app-network
    volumes:
      - .:/app
    stdin_open: true
    tty: true

networks:
  app-network:
    driver: bridge
